# Enforce session recordings

To enforce session recordings in an existing cloud console web app:

If you have not yet created a web app for your cloud console, you must
do so first before enforcing session recordings. See the documentation
for the relevant cloud service provider:

- Create a web app for AWS

- Create a web app for \[%=cv-shared-variables.entra-id-long%\]

- Create a web app for Google Cloud
